### Actividad 1. ¿Basta lógica proposicional?

Para cada enunciado, indicar:

1. Si se puede modelar razonablemente solo con lógica proposicional.  
2. Si requiere lógica de primer orden.  
3. Una justificación breve (1–2 frases).

a) “Si llueve, la calle se moja.”  
b) “Ana es mayor que Luis.”  
c) “Todos los estudiantes entregan tareas.”  
d) “Existe un servidor que no está disponible.”  
e) “Juan es padre de María.” 

***

### Actividad 2. Términos, fórmulas atómicas y expresiones mal formadas

Suponer:

- `Humano` predicado unario,  
- `Padre` predicado binario,  
- `MayorQue` predicado binario,  
- `Disponible` predicado unario,  
- `padre_de` función unaria,  
- `ana`, `luis`, `servidor1` constantes.

Para cada expresión, decidir si es:

- **término**,  
- **fórmula atómica**,  
- **fórmula bien formada no atómica**,  
- **expresión incorrecta**,  

y justificar brevemente.

1. `x`  
2. `ana`  
3. `padre_de(x)`  
4. `Humano(x)`  
5. `Padre(carlos,luis)`  
6. `MayorQue(ana,luis)`  
7. `Padre(x)`  
8. `∀x (Humano(x) → Mortal(x))`  
9. `∃y (MayorQue(y,ana))`  
10. `∀ (Humano(x))` 

***

### Actividad 3. Cuantificadores ∀ y ∃: lectura y sentido

a) Traducir a lenguaje natural claro:

1. `∀x (Persona(x) → Respira(x))`  
2. `∃x (Persona(x) ∧ UsaLentes(x))`  
3. `∀x (Servidor(x) → Monitoreado(x))`  
4. `∃x (Servidor(x) ∧ ¬Disponible(x))` 

b) Explicar con palabras la diferencia de significado entre:

1. `∀x ∃y Ama(x,y)` y `∃y ∀x Ama(x,y)`  
2. `∀x (Estudiante(x) → EntregaTarea(x))` y `∃x (Estudiante(x) ∧ EntregaTarea(x))` 

***

### Actividad 4. Variables libres, ligadas y sentencias cerradas

Para cada fórmula:

1. Indicar qué variables están **ligadas**.  
2. Indicar qué variables están **libres**.  
3. Decir si la fórmula es una **sentencia cerrada** (sin variables libres).

a) `Humano(x)`  
b) `∀x Humano(x)`  
c) `∀x (Humano(x) → Ama(x,y))`  
d) `∃y (MayorQue(x,y) ∧ Persona(y))`  
e) `∀x ∃y ConectadoA(x,y)`  
f) `Padre(x,luis)` 

***

### Actividad 5. Formalización de enunciados

Formalizar los siguientes enunciados usando la notación vista en el documento (predicados tipo `Persona(x)`, `Estudiante(x)`, `Servidor(x)`, etc.): 

a) “Todos los routers están activos.”  
b) “Algún usuario no está autenticado.”  
c) “Ana es mayor que Luis.”  
d) “Cada estudiante entregó alguna tarea.”  
e) “Existe un servidor que no está disponible.”